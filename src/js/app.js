import characterInfo from './characterInfo.js';

const character = {
  name: 'Маг',
  type: 'Magician',
  health: 90,
  level: 13,
  attack: 70,
  defence: 40,
};

console.log(characterInfo(character));
